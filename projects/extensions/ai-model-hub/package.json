{
  "name": "ai-model-hub",
  "version": "1.0.0",
  "description": "AI 模型中心模块",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "openclaw": {
    "extensions": [
      "./plugin/index.ts"
    ]
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^6.0.2"
  }
}
