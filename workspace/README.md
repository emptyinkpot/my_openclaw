# workspace

This repository uses `workspace` as a submodule in the upstream GitHub layout.
On Windows, this directory acts as the visible placeholder so the repo can be
recreated locally.

If you are restoring from GitHub, run `restore-from-github.ps1` from the repo
root to repopulate the expected workspace skeleton.
