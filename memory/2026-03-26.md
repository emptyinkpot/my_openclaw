# 2026-03-26

- OpenClaw desktop shortcut had been pointing directly at Edge and only opened `control-ui-custom/launch.html`, which meant the gateway was not guaranteed to start first. That made the UI look dead when the gateway was not already listening.
- The shortcut has been corrected to launch `E:\Auto\start-openclaw-admin.ps1` through PowerShell, so it now self-elevates when needed and then starts the normal `start-openclaw.bat` gateway flow before opening the UI.
- `C:\Users\ASUS-KL\Desktop\OpenClaw UI.lnk` now targets `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File "E:\Auto\start-openclaw-admin.ps1"` and keeps the custom Vita Atramenti icon.
- The launcher used to open `control-ui-custom/launch.html`, which then visibly redirected to `index.html` and looked like two pages opening. `openclaw-gateway-run.ps1` now opens `control-ui-custom/index.html#token=...&gatewayUrl=...` directly because `index.html` already includes the same bootstrap/token seeding logic.
- `/automation` was still showing a white screen / garbled nav in some browser sessions because the page depended on a client-side `loadSharedNavBar()` fetch path while the plugin route was serving its own HTML. The `automation-hub` plugin now strips that client-side nav loader and injects `/shared/nav-bar.html` server-side for `/automation`, so the route no longer depends on a second fetch to render the shared nav.
- The real live `control-ui` root is `E:\Auto\projects\control-ui-custom`, not the mirrored `extensions/public/control-ui-custom` copy. Fixing the mirrored files alone does not change what `http://127.0.0.1:5000` serves.
- The live shared nav file at `E:\Auto\projects\control-ui-custom\shared\nav-bar.html` had become garbled and even broke the `/automation` anchor HTML. Replacing the live nav with a clean ASCII-safe version removed the broken nav source for the running gateway.
- The restart surfaced a separate root cause: `E:\Auto\projects\extensions\plugins\automation-hub\index.ts` itself no longer parsed (`ParseError ... line 66`), so `/automation` was falling back to the default OpenClaw control page. Rewriting `automation-hub/index.ts` to a clean, parser-safe implementation restored both `/automation` and `/api/automation/*`.
- The shared nav was then switched back from the temporary ASCII-only labels to real UTF-8 Chinese in `E:\Auto\projects\shared\nav-bar.html`, and the running routes now render Chinese nav text again (`设置 / 小说管理 / 自动化 / 项目结构 / 经验中心 / 缓存 / 日志 / 文件 / 控制台`).
- `E:\Auto\projects\extensions\plugins\novel-manager\index.ts` was too corrupted and dependency-heavy to rescue safely, so it was replaced with a minimal parser-safe plugin that server-renders a small direct `/novel` page, injects the shared nav server-side, avoids extra redirects, and exposes `/api/novel/health`.
- Local `typescript@5.3.3` and `@types/node@20.10.0` were installed in `E:\Auto\projects\extensions\plugins\novel-manager` so the rewritten plugin can be syntax-checked locally with `npx tsc`.
- After an elevated restart, the live gateway on port `5000` registered both `novel-manager` and `automation-hub`; `/novel/` now serves the simplified recovery page, `/api/novel/health` returns success, and `/automation` still serves the automation page with the restored Chinese nav.
- The simplified `/novel/` recovery page was then removed because the real intended UI is `E:\Auto\projects\extensions\public\novel-manager.html`. The plugin now serves that existing page directly instead of the temporary placeholder.
- `E:\Auto\projects\extensions\plugins\novel-manager\index.ts` was upgraded again into a recovery-style backend for the existing `novel-manager.html`: it now injects the shared Chinese nav, strips the page's stale built-in settings modal block, and provides prefix-routed `/api/novel/*` endpoints instead of only `/api/novel/health`.
- The recovered `/api/novel/*` layer now returns live local cache-backed data for the page's startup path: `/stats`, `/works`, `/works/:id`, `/fanqie/accounts`, `/fanqie/works`, `/fanqie/scan`, `/config`, and several safe fallback endpoints for state machine / audit / publish / cache / experience tabs, so the page no longer boots into "加载作品列表失败".
- Startup verification on the live gateway confirmed that `/api/novel/stats` returned `works: 8`, `chapters: 444`, `/api/novel/works` returned the cached Chinese work list (including `枪与凋零之花`), `/api/novel/fanqie/accounts` returned both account entries, and the duplicate-base path bug in the frontend is now tolerated because `/api/novel/api/novel/works` is normalized server-side.
- `E:\Auto\projects\extensions\plugins\novel-manager\index.ts` was then reconnected to the real MySQL-backed novel service, and `E:\Auto\projects\extensions\core\config.ts` / `config.js` now default the DB host to `124.220.245.121` instead of the stale Tencent hostname.
- A fresh gateway instance on `http://127.0.0.1:5001` verified the MySQL path end-to-end: `/api/novel/health` reports `mysql-primary-with-safe-fallback`, `/api/novel/stats` returns `works: 3` and `chapters: 600`, and `/api/novel/works/1` returns the real first work (`我的绑定恋人是病娇？？`) plus chapter data (`天国，今日停业`).
- The live `5000` gateway was still an older elevated instance that could not be replaced from the current medium-integrity shell: both `Stop-Process` and `taskkill /PID ... /T /F` failed with `Access is denied`, so code-on-disk and process-in-memory temporarily diverged again.
- To keep `http://127.0.0.1:5000/novel/` usable without adding redirects, `E:\Auto\projects\extensions\public\novel-manager.html` now auto-probes the novel API health endpoint and silently falls back to `http://127.0.0.1:5001/api/novel` whenever the same-origin `5000` gateway is still in recovery mode. The `5001` API already passes CORS preflight from `5000`.
- `E:\Auto\start-openclaw-admin.ps1` and `E:\Auto\openclaw-gateway-run.ps1` now also support a forced replacement path via `-ForceRestart` / `OPENCLAW_FORCE_RESTART=1`, so the next UAC-approved elevated restart can actually evict the stale `5000` listener instead of always reusing it just because port `5000` is occupied.
- A top-level modularization plan for `E:\Auto\projects\extensions` was saved to `E:\Auto\projects\extensions\ARCHITECTURE.md`. The accepted direction is now: no `modules/`, no compatibility-layer directories, root-level `core/plugins/public` are temporary only and must be deleted after migration, `kernel` stays business-free, and each real module owns its own frontend/backend/core/contracts/plugin.
- `E:\Auto\projects\extensions\ARCHITECTURE.md` was expanded into an execution-grade migration spec with a full target tree, full runtime tree, a 96-step migration sequence, a "do not make these mistakes" section, and a per-module acceptance checklist so later implementation can follow one canonical plan instead of drifting.
- Further architecture review confirmed that `E:\Auto\projects\control-ui-custom` and `E:\Auto\projects\extensions\public\control-ui-custom` were byte-identical duplicate trees. The canonical control UI source is the project-root `control-ui-custom`, while the `extensions/public` copy is only a stale mirror; the architecture plan now explicitly marks it for deletion, forbids `gateway.controlUi.root` from pointing into `extensions`, and requires sync scripts to stop writing the mirrored copy.
- Control UI consolidation was executed for real: `E:\Auto\projects\shared\sync-nav-bar.sh` no longer syncs into `extensions/public/control-ui-custom`, the mirror directory under `extensions/public` was removed, and that exposed a hidden problem that `E:\Auto\projects\control-ui-custom` had actually been a junction pointing at the mirror. The junction was replaced with a real directory restored from the recycle bin, so `control-ui-custom` is now a true on-disk source directory again and `http://127.0.0.1:5000/control-ui-custom/index.html`, `/launch.html`, and `/chat` all returned 200 during verification.
- `E:\Auto\projects\extensions\plugins\novel-manager\index.ts` was further repaired so the `📦 资源库` and `✨ AI 自动处理` areas no longer use placeholder responses. The plugin now reads `vocabulary` / `literature` / `banned_words` directly from MySQL, performs real banned-word replacement, lazy-loads the real `content-craft` / `audit` / `publish` / `smart-scheduler` services through a local `ts-node` runtime, and normalizes activity-log IDs so the frontend log stream does not miss updates.
- `E:\Auto\projects\extensions\core\smart-scheduler\ActivityLog.js` was rewritten to a clean runtime-safe implementation because the stale generated JS lacked publish-log methods and later caused syntax/runtime failures when `PublishAutoService` started.
- A fresh non-elevated OpenClaw gateway on `http://127.0.0.1:5001` was restarted against the new code and verified end-to-end: `/api/novel/main-library` returned 908 entries, `/api/novel/literature` returned 55 entries, `/api/novel/banned-words` returned 160 entries, `/api/novel/banned-words-replace` replaced `静默 / 冷彻 / 凛冽` with `沉静`, `content-craft-auto` / `publish-auto` / `smart-scheduler` all reported `running: true` after start, and `/api/novel/activity-log` returned the expected startup/shutdown entries with numeric IDs.
- `E:\Auto\projects\extensions\ARCHITECTURE.md` was updated with the new migration prerequisite: after `control-ui-custom` consolidation, `novel-manager` resource library and AI automation recovery must be treated as a completed, verified pre-migration stage before formal module extraction continues.
- `publish-auto` was then normalized again so the live frontend now calls the canonical `/api/novel/publish-auto/*` endpoints instead of the duplicated `/api/novel/novel/publish-auto/*` path. The plugin still accepts the old alias, but the page source no longer emits it.
- The first real `novel-manager` module extraction step was executed under `E:\Auto\projects\extensions\novel-manager`: the plugin entry moved to `plugin/index.ts`, the page moved to `frontend/pages/novel/index.html`, the service moved to `backend/services/novel-service.*`, utilities moved to `core/utils/*`, `module.json` and root `index.ts` were created, and `openclaw.json` now loads `E:/Auto/projects/extensions/novel-manager/plugin`.
- Core reverse dependencies were partially corrected so domain code under `E:\Auto\projects\extensions\core\*` now points at `E:\Auto\projects\extensions\novel-manager\*` instead of importing back into `E:\Auto\projects\extensions\plugins\novel-manager\*`.
- The old source directory `E:\Auto\projects\extensions\plugins\novel-manager` was fully drained and then deleted once the new structure was verified.
- Fresh gateways on `http://127.0.0.1:5006` and then `http://127.0.0.1:5001` verified the migrated structure end-to-end: `/api/novel/health` now reports the page source as `E:\Auto\projects\extensions\novel-manager\frontend\pages\novel\index.html`, `/api/novel/main-library` still returns MySQL data, `/api/novel/publish-auto/status` works on the canonical path, and `/novel/` serves from the new module layout.
- `http://127.0.0.1:5000` is still pinned to an older elevated `node` gateway process that the current shell cannot terminate (`Access is denied`). Attempting `E:\Auto\start-openclaw-admin.ps1 -ForceRestart` reached the UAC boundary and was canceled, so the new module structure is live on `5001` but not yet cut over onto the legacy `5000` listener.
- `E:\Auto\projects\extensions\novel-manager\plugin\index.ts` was further slimmed so the module now only owns `/novel`, `/novel/`, `/novel-manager`, `/novel-manager/`, `/novel-manager.html`, and `/novel-home.html`; it no longer registers `/`, `/cache.html`, `/project-structure.html`, `/automation`, or `/experience.html` as novel pages.
- A fresh gateway on `http://127.0.0.1:5008` re-verified the slimmed module: `/novel/` and `/novel-manager.html` both returned the real module page, `/project-structure.html` now fell back to the OpenClaw control shell instead of being hijacked by novel-manager, `/automation` still returned the automation page, and `/api/novel/health` reported `routeMode: minimal`.
- The resource-library endpoints still returned live MySQL data after the route slimming on `5008`: `/api/novel/main-library` returned 908 entries, `/api/novel/literature` returned 55 entries, and `/api/novel/banned-words` returned 160 entries.
- `banned-words-replace` and `preferred-words-replace` were hardened so they no longer hang forever waiting for `coze-coding-dev-sdk`; both routes now timebox the LLM call and gracefully fall back instead of blocking the page. A live `5008` check confirmed `/api/novel/banned-words-replace` now returns promptly in `fallback` mode for `APP CEO KPI`, replacing them with `著述 / 江河 / 江河`.
