# 2026-03-27

- 重新按工作区启动约定读取了 `SOUL.md`、`USER.md`、最近两天的 `memory`，把上下文接回到当前主线。
- `novel-manager` 的 `plugin id mismatch` 根因确认不是源码业务逻辑，而是 OpenClaw 插件根目录选错：
  - 之前 `plugins.load.paths` 指向 `E:/Auto/projects/extensions/novel-manager/plugin`
  - OpenClaw 会把这个目录名 `plugin` 当成 `idHint`
  - manifest 的 `id` 是 `novel-manager`
  - 因而触发 `plugin id mismatch (manifest uses "novel-manager", entry hints "plugin")`
- 修复方案已经落地：
  - `E:\Auto\projects\openclaw.json` 的 `plugins.load.paths` 改为 `E:/Auto/projects/extensions/novel-manager`
  - `E:\Auto\projects\extensions\novel-manager\openclaw.plugin.json` 上提到模块根
  - `E:\Auto\projects\extensions\novel-manager\package.json` 新增 `openclaw.extensions: ["./plugin/index.ts"]`
  - 原 `E:\Auto\projects\extensions\novel-manager\plugin\openclaw.plugin.json` 已删除，避免双份插件清单
- `feishu-openclaw-plugin` 的缺依赖问题也已确认并修复：
  - 在 `E:\Auto\projects\extensions\plugins\feishu-openclaw-plugin` 执行了 `npm install`
  - 补齐了该插件自己的依赖树，不再缺 `@sinclair/typebox`
- 依赖补齐后暴露出第二个真实问题：
  - `feishu-openclaw-plugin/openclaw.plugin.json` 与 `openclaw.json` 都使用 `feishu-openclaw-plugin`
  - 但 `index.js` 默认导出的 `plugin.id` 写成了 `feishu`
  - 本地已把 `E:\Auto\projects\extensions\plugins\feishu-openclaw-plugin\index.js` 的导出 id 改为 `feishu-openclaw-plugin`
- 使用项目配置 + 独立临时 state 起了两轮隔离网关复测：
  - `5011`：验证 `novel-manager` 告警已消失，但还看到 Feishu 的 `config/export id mismatch`
  - `5012`：打上 Feishu 本地补丁后再次启动，告警消失，Feishu 工具、命令与 WebSocket 通道均成功注册
- `5012` 复测结果：
  - `/automation` 返回 `200`
  - 带 token 访问 `/api/novel/health` 返回成功，`routeMode: minimal`
  - `/api/novel/main-library` 返回 `908`
  - `/api/novel/literature` 返回 `55`
  - `/api/novel/banned-words` 返回 `160`
  - `/api/novel/banned-words-replace` 对 `APP CEO KPI` 返回 `fallback`，替换为 `著述 / 江河 / 江河`
- 本轮临时测试实例 `5011` / `5012` 已主动关闭，没有动现存的旧 `5000` 和昨晚的 `5001`。
- `E:\Auto\projects\extensions\ARCHITECTURE.md` 已补入新的硬约束：
  - OpenClaw 的插件根必须是模块根
  - `plugin/index.ts` 是内部入口，不是 `plugins.load.paths` 里的根
  - 后续所有模块迁移都要沿用这条模式
- automation-hub 模块在隔离实例 `5015` 上完成了第二轮真实验收：
  - `/automation` 返回 200
  - `/api/automation/health` 返回正确模块来源
  - `/api/automation/schedules/save` 与 `/api/automation/schedules` 可写可读
  - 旧 `/auto.html` 已确认不再由 automation-hub 接管，而是落回 OpenClaw 控制壳
- novel-manager 的资源库与 AI 自动处理在同一隔离实例上再次复验：
  - `/api/novel/main-library` = 908
  - `/api/novel/literature` = 55
  - `/api/novel/banned-words` = 160
  - `/api/novel/banned-words-replace` 对 `APP CEO KPI` 返回真实兜底替换
  - `content-craft-auto` / `audit-auto` / `smart-scheduler` / `publish-auto` 都能启动后再正常停止
- 复验时发现 `activity-log` 仍混有字符串 ID；本轮未去硬改底层脏文件，而是在 `novel-manager/plugin/index.ts` 的 `/api/novel/activity-log` 出口统一把日志 ID 归一成稳定数字，重新起 `5015` 后验证通过。
- 重新起 `5015` 后再次确认：
  - `/automation` 仍正常
  - `/api/novel/main-library` 仍正常
  - `/api/novel/activity-log` 现在返回的 `id` 已统一为数字
- 当前下一优先迁移目标固定为 `experience-manager`，因为它仍停留在旧 `plugins/experience-manager` 路径，且 `novel-manager` 还代管着 `/api/novel/experience/*`，这会持续破坏模块边界。
- `experience-manager` 已完成模块根迁移后的真实复测：
  - 先跑 `npx tsc -p E:\Auto\projects\extensions\experience-manager\tsconfig.json --noEmit`，类型检查通过
  - 第一次隔离实例 `5016` 发现页面与 API 仍未注册，日志直接给出根因：插件加载阶段被 `experience-manager/backend/app.ts` 里的 `app.options('*', ...)` 触发的 `Missing parameter name at index 1: *` 打断
  - 已把这段改成按请求方法兜底处理 `OPTIONS`，避免运行时路由解析直接炸掉
  - 之后用全新独立 state 在 `5017` 复测通过：`/experience`、`/api/experience/health`、`/stats`、`/records`、`/notes`、`/columns` 全部返回成功
  - `/experience` 页面里共享导航已正常注入，`经验中心 / 小说管理 / 自动化` 为正常中文，没有再回落到 OpenClaw 默认控制页
  - `novel-manager` 前端里已确认继续走 `experienceApi('/...') -> /api/experience/*`，不再偷回 `/api/novel/experience/*`
- 本轮再次用隔离实例 `5017` 补验 `novel-manager`：
  - `/api/novel/literature`、`/api/novel/banned-words`、`/api/novel/activity-log` 正常
  - `/api/novel/banned-words-replace` 使用 `POST` 时返回真实替换结果：`APP CEO KPI -> 著述 江河 江河`
  - `content-craft-auto` / `audit-auto` / `smart-scheduler` / `publish-auto` 都已实际启动并成功停止
- `E:\Auto\projects\extensions\ARCHITECTURE.md` 的“当前验收状态”已同步更新：
  - `experience-manager` 从“下一优先迁移目标”改为“已完成首轮正式迁移并通过隔离验收”
  - 新增一条迁移经验：不能只看 `tsc`，必须补“隔离端口 + 独立 state + 页面/API 真验收”，否则会漏掉这种插件加载阶段的运行时错误
- `extensions` 级统一验收模板已经正式落地：
  - 新增 `E:\Auto\projects\extensions\package.json`
  - 新增 `E:\Auto\projects\extensions\kernel\testing\module-acceptance.mjs`
  - 新增 `E:\Auto\projects\extensions\kernel\testing\module-acceptance.presets.json`
  - 统一命令为 `npm run accept:module -- --module <module-id>`
- 模板首轮自测时又抓出一个真实工程问题：
  - 三个已迁模块本地 `typescript` 版本不统一，导致 `ignoreDeprecations: "6.0"` 在某些本地 `tsc` 上直接报错
  - 已把 `experience-manager`、`automation-hub`、`novel-manager` 的本地 TypeScript 统一到 `6.0.2`
  - `experience-manager` 缺失本地 TS 工具链的问题也已补齐
  - `npm install` 在模块目录下会被 `E:\Auto\projects` 的 workspace 根拖进去并触发 arborist bug，因此后续模块内补依赖要用 `npm install --workspaces=false`
- 新模板已在两个真实模块上复验通过：
  - `experience-manager`
  - `automation-hub`
  - 两者都通过了：类型检查、隔离网关启动、页面/API 验收、旧路径扫描
- 下一业务模块已固定为 `file-manager`，当前边界盘点确认到的真实问题：
  - 仍停留在 `extensions/plugins/file-manager`
  - 前端硬编码 `/file-manager/api/*`
  - `index.ts` 同时承担页面注册、API、文件监听、规则执行、CLI，耦合过高
  - 页面已有乱码文案，且没走统一共享导航注入
  - `workspaceRoot` 直接写死为 `/workspace/projects`
  - 文件读取接口缺少明显边界防护
- `control-ui-custom` 原生界面黑屏的真实根因再次确认不是页面缺失，而是工作区里的 `E:\Auto\projects\control-ui-custom\assets\index-UvgeZ3yV.js` 被编码污染后出现多处语法损坏。
- 直接逐点修补 minified bundle 很快会失控，因为损坏不止一两处；最终采用的稳定方案是仅恢复这一个主 bundle 到 Git `HEAD` 里的健康版本，保留 `index.html` 与 `bootstrap.js` 的现有本地改动不动。
- 2026-03-27 当天用 Edge + Playwright 实测 `http://127.0.0.1:5000/control-ui-custom/index.html` 与 `/launch.html`，确认已不再黑屏、无 `pageerror`，页面能正常渲染到登录/连接界面；剩余仅是 `token missing` 与 `favicon/config` 的 404 次级问题。
- 2026-03-27 再次现场复查确认：`experience-manager` 与 `file-manager` 的模块代码修复本身已经成立，不是“磁盘没改好”。本轮重新在模块目录下复跑了本地 `tsc --noEmit`，并用 `npm run accept:module -- --module experience-manager`、`npm run accept:module -- --module file-manager` 分别在隔离端口 `5353`、`5293` 通过验收。
- 当前唯一主阻塞已经收敛得很清楚：旧高权限 `node` 进程 `PID 25224` 仍占着 `5000`，导致 `http://127.0.0.1:5000/api/experience/health` 与 `/api/file-manager/health` 仍是 `404`，浏览器弹窗也仍然指向旧 Linux 风格目录 `/workspace/projects/browser/fanqie-account-1`。现在的问题是“旧进程没有被替换”，不是“模块代码仍然坏着”。
- `E:\Auto\projects\extensions\ARCHITECTURE.md` 开头已改写成带实测证据的“线上错误复查与收尾计划”：
  - 补上了当前在线拓扑树：`5000 -> PID 25224`、`5001 -> PID 12584`
  - 补上了今天这轮重新复跑的隔离验收结果
  - 明确后续顺序先走管理员链路替换 `5000`，再做在线复验，再处理兼容层与历史脚本文本残留
  - 不再把已经扫掉的 `novel-manager/backend/services/novel-service.ts`、`novel-manager/plugin/index.ts`、`experience-manager/core/MemorySync.ts` 误写成当前主阻塞
- 夜间继续处理 `5000` 启动链路时，确认 `openclaw-gateway-run.ps1` 有两层真实问题：
  - `Stop-StaleGatewayListener` 把监听进程写进 `$pid`，触发 PowerShell 只读变量冲突
  - 即使新网关已经成功拉起，父脚本仍可能因等待判定过窄而误报“Gateway did not start”
- 已把 `E:\Auto\openclaw-gateway-run.ps1` 修成：
  - 使用 `$listenerPid`，不再碰 `$PID`
  - 启动前自动清理同配置 stale lock
  - 监听检测补成“端口监听 + TCP 探测 + HTTP 就绪”三层组合
  - 启动等待窗口扩到 45 秒，并把关键失败提示改成中文
- 已做完整真验收：
  - 手动停掉当前 `5000` 实例后重新执行启动脚本，返回 `exit 0`
  - `5000` 恢复监听
  - `/api/novel/health`、`/api/automation/health`、`/api/experience/health`、`/api/file-manager/health` 全部通过
  - `curl http://127.0.0.1:5000/automation` 返回的 HTML 已含中文导航 `小说管理 / 自动化`
- 这意味着当前主阻塞已从“收回 `5000`”转为“在已恢复的 `5000` 基础上继续正式迁移计划”。
- 在继续迁移前又完成了一轮“线上收尾”：
  - `experience-manager/scripts/git-post-commit.sh` 不再写死 `/workspace/projects/...`，改成从 Git 仓库根推导经验文件路径
  - `core/config.ts` / `core/config.js` 现在默认把 `paths.root` 解析到 `extensions/novel-manager`，SQLite 默认路径也改到了模块目录
  - `automation-hub/frontend/pages/automation/index.html` 去掉了前端自取共享导航脚本，旧 `auto-scripts` 文案改成了 `automation-hub`，数据库主机默认值改到 `124.220.245.121`
  - `extensions/public/nav-bar.html` 与 `nav-bar-behavior.js` 已同步到 `shared` 和 `control-ui-custom/shared`，不再高亮旧 `/auto.html` / `/automation.html`
  - `novel-manager/plugin/index.ts` 已移除 `/novel-manager*` 和 `/novel-home.html` 历史页面别名
- 重启 `5000` 后再次在线复验：
  - `/api/novel/health` 的 `pageRoutes` 现在只剩 `/novel` 与 `/novel/`
  - 旧 `/novel-manager.html` 不再回到小说页，而是落回 OpenClaw 控制壳
  - `/api/automation/health`、`/api/experience/health`、`/api/file-manager/health` 继续正常
- 已开始下一阶段盘点 `ai-model-hub`，确认它当前仍停留在 `extensions/plugins/ai-model-hub`，且还保持旧式 `/ai-hub -> localhost:5100` 代理模式与 `API_BASE='/ai-model/api'` 的不一致结构。
- 重新在正式 `5000` 上做了浏览器级真验收，不再只看状态码：
  - `automation?view=feishu&tab=chat`
  - `novel/?page=cache-manage`
  - `novel/?page=logs-view`
  - `experience`
  - `file-manager`
  - `ai-hub`
  - 以上页面都能正常渲染、导航为正常中文、无新的 `pageerror`
- 这轮又抓到一个“页面看起来能用但其实仍在报错”的真实前端问题：
  - `http://127.0.0.1:5000/novel/?page=logs-view` 主日志区域已经能显示
  - 但控制台仍报 `ReferenceError: getStatusLabel is not defined`
  - 页面右下角还会弹 `状态机数据加载失败`
  - 已在 `E:\Auto\projects\extensions\novel-manager\frontend\pages\novel\index.html` 补回 `getStatusLabel()`，重启 `5000` 后复测通过
- 这轮还确认了一个导航层面的假正常问题：
  - 共享导航里的 `/project-structure.html` 访问虽然返回 `200`
  - 但实际回的是默认 OpenClaw 控制壳，不是正式模块页面
  - 已从 `shared/nav-bar.html`、`control-ui-custom/shared/nav-bar.html`、`extensions/public/nav-bar.html` 统一移除该入口
- 顶层 `extensions/public` 当前已经只剩共享导航相关文件：
  - `nav-bar.html`
  - `nav-bar.js`
  - `nav-bar-behavior.js`
  - 旧 `cache.html` / `logs.html` / `publish.html` / `feishu.html` 已确认不再存在
- `E:\Auto\projects\extensions\ARCHITECTURE.md` 与 `CURRENT-KNOWLEDGE.md` 已按最新线上状态重写：
  - 用 `5000` 当前快照 `PID 3044` 替换旧快照
  - 把阶段 A 改成“顶层 public 历史业务页已收口完成”
  - 下一阶段明确改为：先收口顶层 `core` 与模块内运行态，再收口顶层 `public` 壳资源本身，再补 `novel-manager/contracts`，再迁 `feishu-openclaw-plugin`
