2026-03-29

- Fixed a VS Code Roo Code stale-runtime issue where `E:\MyBlog` window resumed an old task with an outdated API key after disk config had already been migrated.
- Confirmed Roo 3.51.1 reads `C:\Users\ASUS-KL\.roo\.env.local` through its bootstrap path, but stale in-memory extension-host state can still keep using an old key until that specific extension host is restarted.
- Unified Roo-related secret storage, global state, and task history to the newer proxy key/base/model, then restarted only the affected VS Code extension host instead of closing all Code windows.
- After restart, `window21` / `E:\MyBlog` no longer produced new old-key (`cr_75927...`) log entries; remaining failures before restart were mixed `402` proxy quota and `401` stale resumed-task errors.
- Found an additional root cause after the first restart: VS Code global state key `RooVeterinaryInc.roo-cline` had silently reverted `currentApiConfigName` back to `codex-openai`, so fresh `startTask` calls could still pick the old key even though `.env.local` was already updated.
- Fixed that second layer by updating `C:\Users\ASUS-KL\AppData\Roaming\Code\User\globalStorage\state.vscdb` to `currentApiConfigName=env_global`, rewriting Roo's own `globalStorage` task history from `codex-openai` to `env_global`, then restarting the affected extension host again.
- Later replaced the patchy storage-hook approach with a simpler direct runtime rewrite: `bootstrap.js` now only loads `C:\Users\ASUS-KL\.roo\.env.local`, and Roo's bundled OpenAI provider constructor in `dist/extension.js` was rewritten to force the correct proxy/key/model at client-init time.
- Confirmed the proxy behavior: `https://apikey.soxio.me/openai/chat/completions` returns `404`, while `https://apikey.soxio.me/openai/v1/chat/completions` and `https://apikey.soxio.me/openai/responses` both work with the new key.
- New Roo task `019d396d-3a79-76f8-adeb-ebe9836fea25` completed successfully after the direct runtime rewrite, so the old-key `401` issue is resolved for fresh tasks; stale broken history items may still need pruning if Roo tries to auto-resume them.
